<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%= form.label :item_type_id %><br>
    <%= collection_select(:item, :item_type_id, ItemType.all, :id, :name, include_blank: true) %>
  </div> -->

  <div class="field">
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %><br>
    <%= form.date_select :expiration_date, order: [:month, :day, :year], default: nil, class: 'form-control' %>
  </div>

  <div class="actions text-center">
    <%= form.submit class: 'btn btn-primary'%>
  </div>
<% end %>
